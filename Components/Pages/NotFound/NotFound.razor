@page "/not-found"
@attribute [AllowAnonymous]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/error/404", replace: true);
    }
}
